<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartshop.mapper.UserMapper">

    <resultMap id="userMap" type="com.smartshop.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="enabled" column="enabled"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="userMap">
        SELECT id, username, password, role, enabled, create_time
        FROM user
        WHERE username = #{username}
    </select>

    <select id="findAll" resultMap="userMap">
        SELECT id, username, password, role, enabled, create_time
        FROM user
        ORDER BY id DESC
    </select>

    <select id="findById" resultMap="userMap">
        SELECT id, username, password, role, enabled, create_time
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, role, enabled, create_time)
        VALUES (#{username}, #{password}, #{role}, #{enabled}, NOW())
    </insert>

    <update id="update">
        UPDATE user
        SET username = #{username},
            role = #{role},
            enabled = #{enabled},
            update_time = NOW()
        <if test="password != null and password != ''">
            , password = #{password}
        </if>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>